import React from "react";

const Navigation = () => {
    return (
        <nav style={{textAlign: "right", paddingRight:"20px", fontWeight:'700', cursor:'pointer'}}>
            <p style={{fontSize: "20px", textDecoration: 'underline'}}>Sign Out</p>
        </nav>
    )
};

export default Navigation;